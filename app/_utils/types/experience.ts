export type Experience = {
  title: string;
  description?: string;
  dates: string;
  bullets: string[];
  source?: string;
  avatarSource?: string;
  avatar?: string;
  links?: { text: string; href: string }[];
};
